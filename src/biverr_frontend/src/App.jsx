import { useState } from 'react';

function App() {

  function handleSubmit(event) {
   event.preventDefault();
  }

  return (
    <main>
      <span>Hello world</span>
    </main>
  );
}

export default App;
